import { getL1Provider } from "@/config/l1Provider";
import { Contract, ContractInterface } from "ethers";

export function useCallEthersContract(params: {
  address: `0x${string}`;
  abi: ContractInterface;
}) {
  // console.log(getL1Provider());
  // const test = new Contract(
  //   getL1TestProvider()
  // );
}
